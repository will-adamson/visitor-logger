# visitor-logger
A small project that links embedded design practices with a database and image recognition


## Set up

Run `python3 main.py &`
Then run `python3 visitor_stats.py`

To close both you can close visitor_stats by using ctrl + c, but then you need to run `fg` to bring the application in the background to the front so it can be closed the same way.
